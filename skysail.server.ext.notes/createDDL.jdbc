CREATE TABLE SKYSAIL_SERVER_EXT_NOTES_NOTES (ID INTEGER NOT NULL, CHANGED DATETIME, CONTENT VARCHAR(255), CREATED DATETIME, TITLE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SKYSAIL_SERVER_EXT_NOTES_FOLDER (ID INTEGER NOT NULL, NAME VARCHAR(255), NOTES LONGBLOB, PATH VARCHAR(255), SUBFOLDERS LONGBLOB, PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)

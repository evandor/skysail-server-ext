ul(list) ::= <<
  <p>
      $list:{ element | $li(element,i)$}$
  </p>
>>

li(element,i) ::= <<

    $header(element)$<br />
    
>>

header(map)       ::= "<i class='icon-folder-close'></i>&nbsp;<a href='folders/$map.folderName$'>$map.folderName$</a>"
